query CategoriesList($first: Int!) {
	categories(first: $first) {
		edges {
			node {
				id
				name
				slug
				description
				backgroundImage {
					url
					alt
				}
				products {
					totalCount
				}
			}
		}
	}
}
